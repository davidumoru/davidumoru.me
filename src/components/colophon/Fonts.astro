---

---

<p class="serif">Serif: Instrument Serif</p>
<p class="sans">Sans: Inter</p>
<p class="mono">Mono: Geist Mono</p>

<style>
  p {
    margin: 0;
  }
  p + p {
    margin-block-start: var(--space-xs);
  }

  .serif {
    font-family: var(--font-instrument-serif), serif;
    font-size: var(--fs-lg);
  }
  .sans {
    font-family: var(--font-inter), sans-serif;
  }
  .mono {
    font-family: var(--font-geist-mono), monospace;
    font-size: var(--fs-sm);
  }
</style>
